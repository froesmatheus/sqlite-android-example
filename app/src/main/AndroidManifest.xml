<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seminfo.br.agenda" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.TelaInicial"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CadastrarContatos"
            android:label="@string/title_activity_cadastrar_contatos"
            android:parentActivityName=".activities.TelaInicial">
        </activity>
        <activity
            android:name=".activities.AtualizarContatos"
            android:label="@string/title_activity_atualizar_contatos" >
        </activity>
        <activity
            android:name=".activities.ListaContatos"
            android:label="@string/title_activity_lista_contatos"
            android:parentActivityName=".activities.TelaInicial">

        </activity>
    </application>

</manifest>
